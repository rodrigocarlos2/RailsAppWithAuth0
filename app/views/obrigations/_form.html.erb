
<%= form_for(obrigation) do |f| %>
  <% if obrigation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(obrigation.errors.count, "error") %> prohibited this obrigation from being saved:</h2>

      <ul>
      <% obrigation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %>
    <%= f.text_area :content %>
  </div>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
